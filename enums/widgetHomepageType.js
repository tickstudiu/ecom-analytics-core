/**
 * Enum for widget type for GA4
 * @readonly
 * @enum {String}
 */
export default Object.freeze({
	HERO_BANNER: 'Hero Banner',
	HIGHLIGHT_BANNER: 'Promotion Banner 2',
	SMALL_BANNER: 'Promotion Banner',
});
