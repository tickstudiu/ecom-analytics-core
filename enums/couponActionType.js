/**
 * Enum for action type to add coupon to cart
 * @readonly
 * @enum {String}
 */
export default Object.freeze({
	CLICKED_BUTTON: 'clicked_button',
	INPUT_COUPON: 'input_coupon_code',
	AUTO_APPLY: 'auto_apply', // auto apply coupon by API
});
